# --- Web API ---
fastapi
uvicorn[standard]
python-multipart

# --- LLM & Schema Validation ---
openai
instructor
pydantic

# --- Document Parsing (The Aligned Ecosystem) ---
# We must pin OpenCV to avoid it forcing Numpy 2.0
opencv-python-headless==4.10.0.84
numpy==1.24.4

# We must pin the 3.2.2 engine to avoid the 3.3.0 C++ OneDNN bug
paddlepaddle==3.2.2

# We must pin both OCR wrappers to the exact same 3.0.0 version
paddleocr==3.0.0
